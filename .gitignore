# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# ÜrTC private
"eggs benedict"/
eggs benedict/
GROWTH-PLAYBOOK.md
*.bak
*.bak2
*.tmpbak
.firebase/
functions/node_modules

# Android signing keys — NEVER commit
*.jks
*.keystore

# Secrets — never commit env files (a real key leaked from .env once already)
.env
.env.*
!.env.example
.env 2

# Confidential company documents — governance agreements, IP assignments,
# NDAs and strategy/financial models. These live in Drive, never in git.
*.docx
*.pdf
